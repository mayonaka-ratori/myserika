---
description: "ボット操作: status | start | stop | restart | logs [行数]"
allowed-tools:
  - Bash
model: claude-haiku-4-5-20251001
---

以下のコマンドを実行して結果を**そのまま**表示してください。

```bash
python C:/Users/hosom/my-secretary/scripts/bot_control.py $ARGUMENTS
```

引数なしで呼ばれた場合は `status` として実行してください：
```bash
python C:/Users/hosom/my-secretary/scripts/bot_control.py status
```

出力結果だけを返してください。余分なコメントや説明は不要です。
